import SidebarLeft from "./widgets/sidebar";

type Props = {};

const EditorPage = (props: Props) => {
  return (
    <div {...props}>
      <SidebarLeft />
    </div>
  );
};

export default EditorPage;
